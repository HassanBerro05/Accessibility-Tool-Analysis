SELECT

id,

(json_each(report_json_cleaned::json->'error_summary')).key AS key, ((json_each(report_json_cleaned::json->'error_summary')).value)::text AS value FROM reports
WHERE json_typeof(report_json_cleaned::json->'error_summary') = 'object'

ORDER by id


